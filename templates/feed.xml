<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ title }}</title>
  <updated>{{ updated }}</updated>
  <id>{{ site_url }}</id>

  {% for (uri, data) in links %}
  <entry>
    <title>{{ data.title }}</title>
    <link href="{{ uri }}"/>
    <id>{{ uri }}</id>
    <updated>{{ data.added }}</updated>
    <summary>{{ data.description }}</summary>
  </entry>
  {% endfor %}

</feed>
